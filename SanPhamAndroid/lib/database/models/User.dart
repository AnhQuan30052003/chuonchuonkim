// Sau khi hoàn thành sản phẩm, code đăng nhập thì sẽ tới giỏ hàng !

class User {
  String id, ten, email, sdt;

  User({
    required this.id,
    required this.ten,
    required this.email,
    required this.sdt,
  });
}